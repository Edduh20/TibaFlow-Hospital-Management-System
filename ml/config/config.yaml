project:
  name: ml-project
  version: "0.1.0"

data:
  raw_path: data/raw/
  processed_path: data/processed/
  artifact_path: artifacts/

model:
  save_path: models/
  best_model: models/best_model.pkl

logging:
  log_dir: logs/
  level: INFO
